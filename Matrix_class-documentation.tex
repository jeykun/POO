\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[left=2cm,right=2cm,top=2cm,bottom=2cm]{geometry}
%\usepackage[portuguese]{babel}
\usepackage{amsmath,amsfonts,amssymb}
\title{Documentation}
\author{Junior R. Ribeiro}
%\date{}
\usepackage[dvipsnames]{xcolor}
\usepackage{txfonts,classdiagram}
\usepackage[hidelinks]{hyperref}
\hypersetup{
colorlinks = true,
urlcolor = blue,
linkcolor = blue,
citecolor = blue,
}
%\setlength{\parskip}{12pt}
%\setlength{\parindent}{12pt}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% defs
\DeclareMathOperator{\trace}{\mathbf{tr}}
\DeclareMathOperator{\expm}{\mathbf{expm}}
\newcommand{\vb}[1]{\texttt{#1}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{document}

\maketitle
\listoftables

%\changeclasstext[Classe]
%\changelegendtext[Legenda]
%\changeclassendstext[termina aqui.]
%\changecontinuedfromprevious[continuação da página anterior]
%\changecontinuedonnext[continua...]
%\changedescriptcolor[colorname]

\class{\_\_workspace\_\_}
 & (empty)
\xidescript{
there is no global constants on workspace 
 }
\methods
+ & \texttt{warn(msg,endline*)}
\descript{
This function prints "WARN: msg" (red) on terminal.\\
>> msg is a string; \\
>> endline is a boolean.
}
+ & \texttt{info(msg,endline*)}
\descript{
This function prints "INFO: msg" (blue) on terminal.\\
>> msg is a string; \\
>> endline is a boolean.
}
+ & \texttt{rcout(msg,endline*)}
\descript{
This function <<red cout>> prints "msg" (red) on terminal.\\
>> msg is a string; \\
>> endline is a boolean.
}
+ & \texttt{bcout(msg,endline*)}
\xdescript{
This function <<blue cout>> prints "msg" (blue) on terminal.\\
>> msg is a string; \\
>> endline is a boolean.
}
\legends
+ & public\\
* & optional 
\endclass\newpage















\class{Matrix}
\attr[$-$]{me}
\idescript{
Pointer to pointer (the Matrix itself).
}
\attr[$-$]{isdestroyed}
\idescript{
Boolean indicating whether the object was destroyed.
}
\attr[$-$]{m}
\idescript{
The number of rows of the Matrix. A positive integer.
}
\attr[$-$]{n}
\xidescript{
The number of columns of the Matrix. A positive integer.
}
\methods%just before \methods, use a \x(i)descript
$+$&\constructor[m,n]{Matrix}
\idescript{
Constructor method
}
$-$ & \meth[functionName]{throwisdestroyed}
\descript{
This function raises an error and exits the program always when it is attempted to use a destroyed Matrix.\\
>> functionName is a string indicating the name of what function is 	attempting to use the Matrix.
}
$+$&\meth[i,j,value]{set}
\descript{
}
$+$&\meth[i,j]{get}
\descript{
}
$+$&\meth[otherMatrix]{sum}
\descript{
}
$+$&\meth[otherMatrix]{sub}
\descript{
}
$+$&\meth[otherMatrix]{mul}
\descript{
}
$+$&\meth[clearPrompt]{fromuser}
\descript{
}
$+$&\meth{print}
\descript{
}
$+$&\meth{shape}
\descript{
}
$+$&\meth{shape1}
\descript{
}
$+$&\meth{shape2}
\descript{
}
$+$&\meth{destroy}
\xdescript{
}
\legends%just before \legends, use a \x(i)descript
$-$ & private\\
$+$ & public\\
\endclass






%%%%%%%%%%%%%%%%%%%%%%%%%%
%\bibliography{refs.bib}{}%
%\bibliographystyle{plain}%
\end{document}





